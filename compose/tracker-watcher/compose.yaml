services:
  tracker-watcher:
    build: .
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN
      - DISCORD_CHANNEL_ID
      - CHECK_INTERVAL=900
    volumes:
      - state:/data

volumes:
  state:
    driver: local
